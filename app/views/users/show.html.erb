<h1><%= "#{current_user.username}"%> Profile page</h1>

<p>Click <%= link_to "Here", games_path%> to add your review to a game</p>
<h4>My Reviews:</h4>
<ul>
  <% current_user.game_reviews.each do |gr| %>
    <li><%= link_to gr.game.title, user_game_review_path(current_user, gr) %></li>
  <% end %>
</ul>
